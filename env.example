# ============================================
# WANDERLUST PROJECT - ENVIRONMENT VARIABLES
# ============================================
# 
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to git (it's already in .gitignore)
#
# ============================================

# MongoDB Database Connection URL
# --------------------------------------------
# Option 1: MongoDB Atlas (Cloud - Recommended for beginners)
#   - Sign up at: https://www.mongodb.com/cloud/atlas
#   - Create a free cluster
#   - Click "Connect" → "Connect your application"
#   - Copy the connection string and replace <password> with your DB user password
#   - Format: mongodb+srv://username:password@cluster.mongodb.net/wanderlust?retryWrites=true&w=majority
#
# Option 2: Local MongoDB
#   - Install MongoDB locally (brew install mongodb-community on macOS)
#   - Start MongoDB service: brew services start mongodb-community
#   - Format: mongodb://127.0.0.1:27017/wanderlust
#
ATLAS_DB_URL=mongodb+srv://username:password@cluster.mongodb.net/wanderlust?retryWrites=true&w=majority
# ATLAS_DB_URL=mongodb://127.0.0.1:27017/wanderlust

# Session Secret Key
# --------------------------------------------
# Used to encrypt session cookies. Must be a long, random string.
# Generate a secure key using one of these methods:
#   - macOS/Linux: openssl rand -base64 32
#   - Or use any long random string (minimum 32 characters recommended)
# IMPORTANT: Keep this secret and never share it publicly!
#
SECRET_KEY=your-super-secret-key-change-this-to-a-random-string-at-least-32-characters-long

# Cloudinary Configuration (for image storage)
# --------------------------------------------
# Cloudinary is used to store and serve listing images.
# Sign up at: https://cloudinary.com (free tier available)
# After signing up, go to Dashboard to find these values:
#   - Cloud name: Found in your dashboard URL or settings
#   - API Key: Found in Dashboard → Account Details
#   - API Secret: Found in Dashboard → Account Details (click "Reveal")
#
CLOUD_NAME=your-cloudinary-cloud-name
CLOUD_API_KEY=your-cloudinary-api-key
CLOUD_API_SECRET=your-cloudinary-api-secret

